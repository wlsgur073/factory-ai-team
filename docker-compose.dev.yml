services:
  platform:
    build:
      context: .
      dockerfile: docker/Dockerfile.dev
    command: pnpm turbo dev --filter=@nexus/platform
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=development

  codex-web:
    build:
      context: .
      dockerfile: docker/Dockerfile.dev
    command: pnpm turbo dev --filter=@nexus/codex-web
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "5001:5001"
    environment:
      - NODE_ENV=development

  nginx:
    image: nginx:alpine
    ports:
      - "5080:80"
    volumes:
      - ./docker/nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - platform
      - codex-web
